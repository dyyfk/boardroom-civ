# Optional. If unset, the app falls back to a deterministic offline simulator
# so the demo still runs without network access.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-7
BOARDROOM_API_PORT=5181
